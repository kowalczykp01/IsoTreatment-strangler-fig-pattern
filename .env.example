# Copy this file to .env and fill in the real secrets. The .env file is git-ignored.
# Database SA password (used by SQL Server and the services' connection strings)
DB_PASSWORD=
# SMTP password / Gmail app password used to send emails
EMAIL_PASSWORD=
